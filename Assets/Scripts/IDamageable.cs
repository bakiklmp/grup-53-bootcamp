using UnityEngine;

/*public interface IDamageable
{
    void TakeDamage(float amount);
    // You could expand this later with hitPoint, hitDirection etc.
    // void TakeDamage(float amount, Vector3 hitPoint, Vector3 hitDirection);
}*/